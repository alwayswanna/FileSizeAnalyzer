name: disk_space_analyzer
description: Flutter charts sample

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  pie_chart: ^5.1.0
  file_picker: ^4.4.0
  path_provider: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true